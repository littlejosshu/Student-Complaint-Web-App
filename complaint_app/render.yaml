services:
  - type: web
    name: reportsafe-portal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: reportsafe-db
          property: connectionString

databases:
  - name: reportsafe-db
    plan: free
